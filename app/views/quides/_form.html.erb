<%= form_for(@quide) do |f| %>
  <% if @quide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quide.errors.count, "error") %> prohibited this quide from being saved:</h2>

      <ul>
      <% @quide.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :post %><br />
    <%= f.text_field :post %>
  </div>
  <div class="field">
      <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :info %><br />
    <%= f.text_area :info %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
